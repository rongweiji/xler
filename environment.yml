name: xler_py311
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  # Prefer conda packages for numerical and imaging libs when available
  - numpy>=1.24,<2.0
  - scipy>=1.10
  - matplotlib>=3.7
  - pyyaml>=6.0
  - opencv
  - pip:
    - pyserial>=3.5
    - deepdiff>=6.0.0
    - tqdm>=4.65.0
    # Project-specific or PyPI-only packages
    - feetech-servo-sdk>=1.0.0
    - pydualsense
